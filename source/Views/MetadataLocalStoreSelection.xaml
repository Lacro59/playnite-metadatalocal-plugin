<UserControl x:Class="MetadataLocal.Views.MetadataLocalStoreSelection"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:playnitecontrols="clr-namespace:CommonPluginsControls.PlayniteControls"
             xmlns:controls="clr-namespace:CommonPluginsShared.Controls"
             mc:Ignorable="d"
             d:DesignHeight="660" d:DesignWidth="700">

    <Grid>
        <Grid Name="PART_DataLoadWishlist" Visibility="{Binding IsLoadingVisible}">
            <ProgressBar IsIndeterminate="True" Width="350" Height="40" />
            <TextBlock Text="{DynamicResource LOCDownloadingLabel}"
                       Style="{DynamicResource BaseTextBlockStyle}"
                       HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Grid>

        <Grid Name="PART_GridData" Margin="10" IsEnabled="{Binding IsContentEnabled}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="10" />
                <RowDefinition Height="*" />
                <RowDefinition Height="10" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Vertical compact store sidebar -->
            <ListBox Grid.Column="0" Grid.Row="0" Grid.RowSpan="5"
                     ItemsSource="{Binding Stores}"
                     SelectedItem="{Binding SelectedStoreOption}"
                     BorderThickness="0"
                     Background="Transparent"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     MinWidth="110">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                        <Setter Property="Padding" Value="4" />
                        <Setter Property="Margin" Value="0,0,0,6" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="HorizontalAlignment" Value="Stretch" />
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="4,6">
                            <TextBlock Text="{Binding Glyph}"
                                       FontFamily="{DynamicResource CommonFont}"
                                       FontSize="{DynamicResource FontSizeLargest}"
                                       HorizontalAlignment="Center"
                                       Style="{DynamicResource BaseTextBlockStyle}" />
                            <TextBlock Text="{Binding DisplayName}"
                                       FontSize="{DynamicResource FontSizeSmall}"
                                       HorizontalAlignment="Center"
                                       TextAlignment="Center"
                                       TextWrapping="Wrap"
                                       Margin="0,4,0,0"
                                       Style="{DynamicResource BaseTextBlockStyle}" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Search above results -->
            <Grid Grid.Column="2" Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="10" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <playnitecontrols:SearchBox Grid.Column="0"
                                            Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                            KeyUp="SearchElement_KeyUp" />
                <Button Grid.Column="2"
                        Content="{DynamicResource LOCSearchLabel}"
                        Command="{Binding SearchCommand}"
                        MinWidth="80" />
            </Grid>

            <!-- Results list (stretch) -->
            <ListBox Grid.Column="2" Grid.Row="2"
                     ItemsSource="{Binding Results}"
                     SelectedItem="{Binding SelectedResult}"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     HorizontalContentAlignment="Stretch">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="4,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="10" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0"
                                    CornerRadius="{DynamicResource ControlCornerRadius}"
                                    ClipToBounds="True"
                                    Width="140" Height="52">
                                <controls:ImageAsync Source="{Binding ImageUrl}"
                                                     Stretch="UniformToFill" />
                            </Border>
                            <TextBlock Grid.Column="2"
                                       Text="{Binding Name}"
                                       VerticalAlignment="Center"
                                       TextWrapping="NoWrap"
                                       TextTrimming="CharacterEllipsis"
                                       Style="{DynamicResource BaseTextBlockStyle}" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- OK / Cancel -->
            <StackPanel Grid.Column="2" Grid.Row="4"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="{DynamicResource LOCOKLabel}"
                        Command="{Binding OkCommand}"
                        Margin="0,0,10,0"
                        MinWidth="80" />
                <Button Content="{DynamicResource LOCCancelLabel}"
                        Command="{Binding CancelCommand}"
                        MinWidth="80" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
